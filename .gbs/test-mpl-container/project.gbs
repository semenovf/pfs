--************************************************************
--* Generated automatically by `gbs'
--* Command: `gbs prj --create --type=test --name=test-mpl-container'
--* Date:    Sun May  7 12:37:45 2017
--************************************************************

kind          "ConsoleApp"
language      "C++"
targetname    "test-mpl-container"
defines       {  }
includedirs   { "../../include" }
libdirs       { "../../../.build" }
files         { "../../tests/test-mpl-container/*.cpp" }

-- Special for Qt projects
require("../../../.gbs/qt_enable")

--Available modules:
--    core, gui, multimedia, network, opengl, positioning, printsupport,
--    qml, quick, sensors, sql, svg, testlib, websockets, widgets, xml
--
qtmodules     { "core" }

configuration "debug"
    flags        { "FatalWarnings", "Symbols" }
    defines      { "DEBUG" }
    objdir       "../../../.build/pfs-mpl/test-mpl-container/debug"
    targetdir    "../../../.build/tests"
    targetsuffix "-d"
    links        { "pfs-test-d" }

configuration "release"
    flags        { "FatalWarnings" }
    defines      { "NDEBUG" }
    objdir       "../../../.build/pfs-mpl/test-mpl-container/release"
    targetdir    "../../../.build/tests"
    links        { "pfs-test" }


filter "action:gmake"
    buildoptions {}
    linkoptions  {}

include(os.getenv("GBS_HOME") .. "/premake/filter_action_gmake_install")

filter { "debug", "action:gmake" }
    linkoptions  { "-rdynamic" }

-- Special for Qt projects
package.loaded["../../../.gbs/qt_enable"] = nil
